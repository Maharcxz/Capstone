SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your_email@example.com
SMTP_PASS=your_app_password

# Optional: override API port
# PORT=3000
 
# Sender address for emails (use a no-reply mailbox for automated emails)
FROM_EMAIL="Trinity Optimum Vision Center" <noreply@trinityoptinumvisioncenter.shop>